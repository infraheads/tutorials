podTemplate(
    containers: [
        containerTemplate (
            name: "kaniko",
            image: "gcr.io/kaniko-project/executor:debug",
            command: "sleep",
            args: "99999"
        )],
    volumes: [
        secretVolume( 
            mountPath: '/kaniko/.docker/', 
            secretName: 'docker-config'
          )
        ]
      )
      {
        node (POD_LABEL) {
            stage("Get a Golang project") {
                git url: "https://github.com/fortranhub/demo.git", branch: "main"
                container("kaniko") {
                    stage("Build a Golang project") {
                        sh '''
                            /kaniko/executor --dockerfile="./Dockerfile" --context=`pwd` --destination="hakobmkoyan771/demoapp:0.1.0"
                        '''
                    }
                }
            }
        }
    }
